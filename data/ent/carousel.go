// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Tracy-coder/e-mall/data/ent/carousel"
	"github.com/Tracy-coder/e-mall/data/ent/product"
)

// Carousel is the model entity for the Carousel schema.
type Carousel struct {
	config `json:"-"`
	// ID of the ent.
	// primary key
	ID uint64 `json:"id,omitempty"`
	// created time
	CreatedAt time.Time `json:"created_at,omitempty"`
	// last update time
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Product ID | 商品编号
	ProductID uint64 `json:"ProductID,omitempty"`
	// image path | 图片路径
	ImgPath string `json:"ImgPath,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarouselQuery when eager-loading is set.
	Edges        CarouselEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CarouselEdges holds the relations/edges for other nodes in the graph.
type CarouselEdges struct {
	// Owner holds the value of the owner edge.
	Owner *Product `json:"owner,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarouselEdges) OwnerOrErr() (*Product, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: product.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Carousel) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carousel.FieldID, carousel.FieldProductID:
			values[i] = new(sql.NullInt64)
		case carousel.FieldImgPath:
			values[i] = new(sql.NullString)
		case carousel.FieldCreatedAt, carousel.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Carousel fields.
func (c *Carousel) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carousel.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			c.ID = uint64(value.Int64)
		case carousel.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				c.CreatedAt = value.Time
			}
		case carousel.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				c.UpdatedAt = value.Time
			}
		case carousel.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ProductID", values[i])
			} else if value.Valid {
				c.ProductID = uint64(value.Int64)
			}
		case carousel.FieldImgPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ImgPath", values[i])
			} else if value.Valid {
				c.ImgPath = value.String
			}
		default:
			c.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Carousel.
// This includes values selected through modifiers, order, etc.
func (c *Carousel) Value(name string) (ent.Value, error) {
	return c.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the Carousel entity.
func (c *Carousel) QueryOwner() *ProductQuery {
	return NewCarouselClient(c.config).QueryOwner(c)
}

// Update returns a builder for updating this Carousel.
// Note that you need to call Carousel.Unwrap() before calling this method if this Carousel
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Carousel) Update() *CarouselUpdateOne {
	return NewCarouselClient(c.config).UpdateOne(c)
}

// Unwrap unwraps the Carousel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Carousel) Unwrap() *Carousel {
	_tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Carousel is not a transactional entity")
	}
	c.config.driver = _tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Carousel) String() string {
	var builder strings.Builder
	builder.WriteString("Carousel(")
	builder.WriteString(fmt.Sprintf("id=%v, ", c.ID))
	builder.WriteString("created_at=")
	builder.WriteString(c.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(c.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("ProductID=")
	builder.WriteString(fmt.Sprintf("%v", c.ProductID))
	builder.WriteString(", ")
	builder.WriteString("ImgPath=")
	builder.WriteString(c.ImgPath)
	builder.WriteByte(')')
	return builder.String()
}

// Carousels is a parsable slice of Carousel.
type Carousels []*Carousel
